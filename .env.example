# AI Council — Backend API Keys
# Copy this to .env and fill in the keys for backends you want to use.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Ollama needs no API key — just ensure it's running on localhost:11434
# Or configure a custom URL in ~/.ai-council/config.json
